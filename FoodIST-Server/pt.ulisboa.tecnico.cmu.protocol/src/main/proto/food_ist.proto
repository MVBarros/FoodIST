syntax = "proto3";
option java_multiple_files = true;
package pt.ulisboa.tecnico.cmu.protocol;

message PingRequest {
    string ping = 1;
}

message PingResponse {
    string pong = 1;
}

service FoodISTService {
    rpc ping(PingRequest) returns (PingResponse);
}